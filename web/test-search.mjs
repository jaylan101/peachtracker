#!/usr/bin/env node
/**
 * test-search.mjs
 * DEPRECATED — secrets have been removed.
 * Use environment variables and scripts/embed-chunks.mjs for embedding work.
 */
console.error("This script is deprecated. Use environment variables instead.");
process.exit(1);
